name: "CodeQL"

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  schedule:
    - cron: '0 0 * * 0'

jobs:
  analyze:
    name: Analyze (placeholder for Dart/Flutter)
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Initialize CodeQL
      uses: github/codeql-action/init@v3
      # Dart/Flutter is not supported, but this step is here as a placeholder

    - name: No-op Build (Dart/Flutter not supported)
      run: echo "No build needed for Dart/Flutter; CodeQL not supported"

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v3
      # This step will not analyze Dart/Flutter code, but remains for potential future support
